
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  base: '/04colorchanger/',  // replace with your repo name if different
  plugins: [react()]
})
